
import React from 'react';
import { HeroContainer, HeroButton } from './styles';

const HeroSection: React.FC = () => {
  return (
    <HeroContainer>
      <h1>Upgrade Your Space: Exclusive Offers On Home Improvements!</h1>
      <HeroButton>Shop Now</HeroButton>
    </HeroContainer>
  );
};

export default HeroSection;
